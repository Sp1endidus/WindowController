<Window x:Class="WindowController.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Window Controller"
        Height="720"
        Width="1200"
        MinHeight="640"
        MinWidth="1080"
        Background="#F3F8FF">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBrush" Color="#F3F8FF"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PanelAltBrush" Color="#EDF5FF"/>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#1D6FD8"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#1657AE"/>
        <SolidColorBrush x:Key="PrimarySoftBrush" Color="#D9EBFF"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#B8D3F4"/>
        <SolidColorBrush x:Key="TextBrush" Color="#17324F"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#58779C"/>
        <SolidColorBrush x:Key="SelectionBrush" Color="#C9E0FF"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource RoundedButtonStyle}"/>

        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="TextBoxBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="TextBoxBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource RoundedTextBoxStyle}"/>

        <Style x:Key="RoundedCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <BulletDecorator Background="Transparent">
                            <BulletDecorator.Bullet>
                                <Border x:Name="CheckBorder"
                                        Width="18"
                                        Height="18"
                                        CornerRadius="5"
                                        BorderThickness="1.5"
                                        BorderBrush="{StaticResource BorderBrushSoft}"
                                        Background="{StaticResource PanelBrush}">
                                    <Path x:Name="CheckMark"
                                          Visibility="Collapsed"
                                          Margin="3"
                                          Stroke="{StaticResource PrimaryDarkBrush}"
                                          StrokeThickness="2"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          Data="M 1 4 L 4 7 L 10 1"/>
                                </Border>
                            </BulletDecorator.Bullet>
                            <ContentPresenter Margin="8,0,0,0"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </BulletDecorator>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background" Value="{StaticResource PrimarySoftBrush}"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Margin="4"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource PrimarySoftBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource SelectionBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedComboBoxToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent">
                            <Path Width="10"
                                  Height="6"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Fill="{StaticResource PrimaryDarkBrush}"
                                  Data="M 0 0 L 5 6 L 10 0 Z"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource RoundedComboBoxItemStyle}"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="OuterBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"/>
                            <Grid Margin="10,0,8,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="26"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                  Margin="0"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  IsHitTestVisible="False"/>
                                <ToggleButton Grid.Column="1"
                                              ClickMode="Press"
                                              Focusable="False"
                                              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                              Style="{StaticResource RoundedComboBoxToggleButtonStyle}"/>
                            </Grid>
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Slide">
                                <Border Margin="0,4,0,0"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        Background="{StaticResource PanelBrush}"
                                        BorderBrush="{StaticResource BorderBrushSoft}"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                    <ScrollViewer CanContentScroll="True"
                                                  MaxHeight="260">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="OuterBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,12,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="{StaticResource PanelBrush}"
                                BorderBrush="{StaticResource BorderBrushSoft}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="12">
                            <DockPanel>
                                <Border DockPanel.Dock="Top"
                                        Margin="0,0,0,10"
                                        Padding="10,6"
                                        Background="{StaticResource PanelAltBrush}"
                                        CornerRadius="8">
                                    <ContentPresenter ContentSource="Header"
                                                      RecognizesAccessKey="True"
                                                      TextElement.FontWeight="SemiBold"/>
                                </Border>
                                <ContentPresenter/>
                            </DockPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="{StaticResource PanelBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource PanelAltBrush}"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="VerticalGridLinesBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ClipToBounds" Value="True"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource PrimarySoftBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Height" Value="38"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SelectionBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CenteredTextCellStyle" TargetType="TextBlock">
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>

        <Style x:Key="CenteredCheckBoxStyle" TargetType="CheckBox" BasedOn="{StaticResource RoundedCheckBoxStyle}">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="{StaticResource PrimarySoftBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,2"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.RowSpan="6"
                Background="{StaticResource WindowBrush}"
                CornerRadius="14"/>

        <StackPanel>
            <TextBlock Text="Window Controller"
                       FontSize="22"
                       FontWeight="SemiBold"/>
            <TextBlock Margin="0,4,0,0"
                       Foreground="{StaticResource MutedTextBrush}"
                       Text="Controls external windows based on rules in windowrules.json"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,10,0,0"
                Padding="12,10"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="10">
            <TextBlock VerticalAlignment="Center">
                <Run Text="Config: "/>
                <Run x:Name="ConfigPathText"/>
            </TextBlock>
        </Border>

        <GroupBox Grid.Row="2" Header="New / Edit Rule">
            <Grid Margin="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="48"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Corners"/>
                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0">
                    <Button Content="↖" Width="32" Height="32" Padding="0" Click="PresetTopLeft_Click"/>
                    <Button Content="↗" Width="32" Height="32" Padding="0" Margin="6,0,0,0" Click="PresetTopRight_Click"/>
                    <Button Content="↙" Width="32" Height="32" Padding="0" Margin="6,0,0,0" Click="PresetBottomLeft_Click"/>
                    <Button Content="↘" Width="32" Height="32" Padding="0" Margin="6,0,0,0" Click="PresetBottomRight_Click"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Text="Process" Margin="0,10,0,0"/>
                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="8,10,0,0">
                    <TextBox x:Name="ProcessNameInput" Width="180"/>
                    <Button x:Name="PickWindowButton" Content="Pick Window" Margin="8,0,0,0" Click="PickWindowButton_Click"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.Column="3" Text="Title Contains" Margin="0,10,0,0"/>
                <TextBox Grid.Row="1" Grid.Column="4" x:Name="TitleContainsInput" Margin="8,10,0,0"/>

                <TextBlock Grid.Row="2" Grid.Column="3" Text="Exclude Title" Margin="0,10,0,0"/>
                <TextBox Grid.Row="2" Grid.Column="4" x:Name="ExcludeTitleContainsInput" Margin="8,10,0,0"/>

                <TextBlock Grid.Row="3" Text="X" Margin="0,10,0,0"/>
                <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="8,10,0,0">
                    <TextBox x:Name="XInput" Width="80"/>
                    <Button Content="⟸" Width="32" Height="32" Padding="0" Margin="6,0,0,0" ToolTip="Snap Left" Click="PresetSnapLeft_Click"/>
                    <Button Content="⟹" Width="32" Height="32" Padding="0" Margin="6,0,0,0" ToolTip="Snap Right" Click="PresetSnapRight_Click"/>
                </StackPanel>

                <TextBlock Grid.Row="3" Grid.Column="3" Text="Y" Margin="0,10,0,0"/>
                <StackPanel Grid.Row="3" Grid.Column="4" Orientation="Horizontal" Margin="8,10,0,0">
                    <TextBox x:Name="YInput" Width="80"/>
                    <Button Content="⟸" Width="32" Height="32" Padding="0" Margin="6,0,0,0" ToolTip="Snap Top" Click="PresetSnapTop_Click"/>
                    <Button Content="⟹" Width="32" Height="32" Padding="0" Margin="6,0,0,0" ToolTip="Snap Bottom" Click="PresetSnapBottom_Click"/>
                </StackPanel>

                <TextBlock Grid.Row="4" Text="Width" Margin="0,10,0,0"/>
                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="8,10,0,0">
                    <TextBox x:Name="WidthInput" Width="80"/>
                    <Button Content="1/2" Width="42" Margin="6,0,0,0" Click="PresetHalfWidth_Click"/>
                    <Button Content="Full" Width="54" Margin="6,0,0,0" Click="PresetFullWidth_Click"/>
                </StackPanel>

                <TextBlock Grid.Row="4" Grid.Column="3" Text="Height" Margin="0,10,0,0"/>
                <StackPanel Grid.Row="4" Grid.Column="4" Orientation="Horizontal" Margin="8,10,0,0">
                    <TextBox x:Name="HeightInput" Width="80"/>
                    <Button Content="1/2" Width="42" Margin="6,0,0,0" Click="PresetHalfHeight_Click"/>
                    <Button Content="Full" Width="54" Margin="6,0,0,0" Click="PresetFullHeight_Click"/>
                </StackPanel>

                <TextBlock Grid.Row="5" Text="On Top" Margin="0,10,0,0"/>
                <ComboBox Grid.Row="5"
                          Grid.Column="1"
                          x:Name="OnTopInput"
                          Width="110"
                          Margin="8,10,0,0"
                          HorizontalAlignment="Left"
                          SelectedValuePath="Tag"
                          Style="{StaticResource RoundedComboBoxStyle}">
                    <ComboBoxItem Content="-" Tag="{x:Null}"/>
                    <ComboBoxItem Content="Yes" Tag="True"/>
                    <ComboBoxItem Content="No" Tag="False"/>
                </ComboBox>
            </Grid>
        </GroupBox>

        <Border Grid.Row="3"
                Margin="0,12,0,0"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="1"
                ClipToBounds="True">
            <DataGrid x:Name="RulesGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Margin="0"
                      AlternationCount="2">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="1.15*"
                                        ElementStyle="{StaticResource CenteredTextCellStyle}"/>
                    <DataGridTextColumn Header="Title Contains" Binding="{Binding MatchTitleContains}" Width="1.15*"
                                        ElementStyle="{StaticResource CenteredTextCellStyle}"/>
                    <DataGridTextColumn Header="Exclude Title" Binding="{Binding ExcludeTitleContains}" Width="1.55*"
                                        ElementStyle="{StaticResource CenteredTextCellStyle}"/>
                    <DataGridTextColumn Header="X" Binding="{Binding X}" Width="64"
                                        ElementStyle="{StaticResource CenteredTextCellStyle}"/>
                    <DataGridTextColumn Header="Y" Binding="{Binding Y}" Width="64"
                                        ElementStyle="{StaticResource CenteredTextCellStyle}"/>
                    <DataGridTextColumn Header="Width" Binding="{Binding Width}" Width="74"
                                        ElementStyle="{StaticResource CenteredTextCellStyle}"/>
                    <DataGridTextColumn Header="Height" Binding="{Binding Height}" Width="74"
                                        ElementStyle="{StaticResource CenteredTextCellStyle}"/>
                    <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding Enabled}" Width="80"
                                            ElementStyle="{StaticResource CenteredCheckBoxStyle}"/>
                    <DataGridCheckBoxColumn Header="On Startup" Binding="{Binding ApplyOnStartup}" Width="92"
                                            ElementStyle="{StaticResource CenteredCheckBoxStyle}"/>
                    <DataGridTemplateColumn Header="On Top" Width="106">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox SelectedValue="{Binding OnTop, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          SelectedValuePath="Tag"
                                          Width="84"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Style="{StaticResource RoundedComboBoxStyle}">
                                    <ComboBoxItem Content="-" Tag="{x:Null}"/>
                                    <ComboBoxItem Content="Yes" Tag="True"/>
                                    <ComboBoxItem Content="No" Tag="False"/>
                                </ComboBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Actions" Width="136">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Edit" Width="54" Margin="0,0,8,0" Click="EditRule_Click"/>
                                    <Button Content="Delete" Width="54" Background="#6FA8E8" BorderBrush="#6FA8E8" Click="DeleteRule_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <CheckBox x:Name="ApplyAllOnStartupInput"
                          Content="Apply All On Startup"
                          Margin="0,0,16,0"
                          Style="{StaticResource RoundedCheckBoxStyle}"/>
                <CheckBox x:Name="StartInTrayInput"
                          Content="Start In Tray"
                          Style="{StaticResource RoundedCheckBoxStyle}"/>
            </StackPanel>

            <WrapPanel Grid.Column="2"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       ItemHeight="40">
                <Button x:Name="AddOrUpdateButton" Content="Add / Update Rule" Width="170" Margin="0,0,8,0" Click="AddOrUpdateButton_Click"/>
                <Button x:Name="SaveButton" Content="Save Rules" Width="132" Margin="0,0,8,0" Click="SaveButton_Click"/>
                <Button x:Name="ReloadButton" Content="Reload Rules" Width="136" Margin="0,0,8,0" Click="ReloadButton_Click"/>
                <Button x:Name="ApplyNowButton" Content="Apply Now (5s)" Width="146" Margin="0,0,8,0" Click="ApplyNowButton_Click"/>
                <Button x:Name="HideToTrayButton" Content="Hide To Tray" Width="132" Click="HideToTrayButton_Click"/>
            </WrapPanel>
        </Grid>

        <StatusBar Grid.Row="5" Margin="0,12,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Idle"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
